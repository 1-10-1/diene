# Diene

Diene is a work-in-progress Rust game engine built from scratch with a Vulkan renderer backend.

Start with the `diene` facade crate for application-facing APIs, then use the lower-level `diene-engine-*` crates only when you need direct engine integration points.
