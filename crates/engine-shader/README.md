# diene-engine-shader

Backend-agnostic shader compilation for Diene.

This crate compiles Slang shader modules into SPIR-V artifacts that renderer backends can consume without depending on Slang directly.
