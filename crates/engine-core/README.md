# diene-engine-core

Application lifecycle and window orchestration for Diene.

This crate owns backend-agnostic application hosting and drives renderer implementations through `diene-engine-renderer-api`.
