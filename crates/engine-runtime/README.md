# diene-engine-runtime

Runtime application builder and renderer backend selection for Diene.

This crate wires `diene-engine-core` to the compiled renderer backends and provides the public application entry point used by the facade crate.
