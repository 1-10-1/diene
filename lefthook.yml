pre-commit:
  parallel: false
  commands:
    fmt:
      run: cargo fmt --all -- --check
    check:
      run: cargo check --workspace --all-features

pre-push:
  parallel: false
  commands:
    ci:
      run: just ci
