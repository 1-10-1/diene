# Diene

Diene is a work-in-progress Rust game engine built from scratch with a Vulkan renderer backend.

The public entry point is the `diene` facade crate. Lower-level crates are published separately so downstream users can depend on specific engine layers when needed.
